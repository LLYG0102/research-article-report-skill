# 计算方法提炼模板

## 计算部分总览

这篇文章中由计算机完成的部分包括：{数据构建}、{模型/算法}、{训练或搜索}、{候选生成}、{打分过滤}、{统计分析}、{结构/图像处理}。这些步骤与实验验证的连接是：{连接方式}。

## 数据与预处理

作者使用的数据来自{来源}，规模为{数量}，经过{过滤/聚类/去冗余/划分/标准化}。训练、验证、测试或 benchmark 的边界是{划分方式}。如果论文没有披露关键细节，写明缺口。

## 模型或算法

核心方法是{模型/算法名}。输入为{输入对象和特征}，输出为{输出对象}。模型通过{loss/objective/search criterion}学习或优化{目标}。关键创新是{模块/约束/conditioning/表示/训练方式}，它解决了{旧方法瓶颈}。

## 采样、打分与过滤

作者先用{生成/搜索/采样方法}得到候选，再用{打分器/预测器/物理指标/统计阈值}筛选。保留关键阈值、候选数量、每轮过滤规模和最终进入实验的数量。

## 统计分析与可视化

整理显著性检验、置信区间、多重检验、聚类、结构比对、图像重构或曲线拟合方法。说明指标是否真正对应论文主张。

## 与实验的连接

计算结果不是最终结论。说明哪些实验验证了计算设计，哪些实验只是间接支持，哪些结论仍停留在 in silico 层面。
